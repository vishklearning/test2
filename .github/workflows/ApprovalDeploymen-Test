name: ApprovalDeployment
on:
  push:
    branches:[Develop]
  pull_request:
    branches:[Develop]
  workflow_dispatch:

jobs:
 Build:
  runs-on: ubuntu-latest
  steps:
   - uses: actions/checkout@v2
   - name: Compile
      run: echo Hello, world!
